import React from "react";
import { Container } from "react-bootstrap";

function GridExp(props) {
  return (
    <div>
      <h2>{props.title}</h2>
      <p>{props.desc}</p>
    </div>
  );
}

export default GridExp;
